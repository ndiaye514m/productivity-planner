{
  "name": "productivity-planner",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "build:production": "ng build --configuration=production",
    "build:staging": "ng build --configuration=staging",
    "build:development": "ng build --configuration=development",
    "watch": "ng build --watch --configuration development",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch --onlyChanged --verbose",
    "lint": "ng lint",
    "format": "prettier --write .",
    "deploy:development": "npm run build:development && npx firebase-tools use development && npx firebase-tools deploy",
    "deploy:staging": "npm run build:staging && npx firebase-tools use staging && npx firebase-tools deploy",
    "deploy:production": "npm run build:production && npx firebase-tools use production && npx firebase-tools deploy"
    
  },
  "private": true,
  "dependencies": {
    "@angular/animations": "^19.2.7",
    "@angular/common": "^19.2.7",
    "@angular/compiler": "^19.2.7",
    "@angular/core": "^19.2.7",
    "@angular/forms": "^19.2.7",
    "@angular/platform-browser": "^19.2.7",
    "@angular/platform-browser-dynamic": "^19.2.7",
    "@angular/router": "^19.2.7",
    "@ngrx/signals": "^19.0.1",
    "rxjs": "~7.8.0",
    "tslib": "^2.3.0",
    "zone.js": "~0.15.0"
  },
  "devDependencies": {
    "@angular/build": "^19.2.8",
    "@angular/cli": "^19.2.8",
    "@angular/compiler-cli": "^19.2.7",
    "@babel/core": "^7.26.10",
    "@types/jest": "^29.5.14",
    "angular-eslint": "18.4.0",
    "bootstrap": "^5.3.3",
    "bootstrap-icons": "^1.11.3",
    "eslint": "^9.13.0",
    "jest": "^29.7.0",
    "jest-preset-angular": "^14.4.2",
    "jquery": "^3.7.1",
    "ts-node": "^10.9.2",
    "typescript": "~5.5.2",
    "typescript-eslint": "8.10.0"
  },
  "overrides": {
    "esbuild": "^0.25.0",
    "@angular/build": {
      "vite": {
        "esbuild": "^0.25.0"
      }
    },
    "vite": {
      "esbuild": "^0.25.0"
    }
  }
}
